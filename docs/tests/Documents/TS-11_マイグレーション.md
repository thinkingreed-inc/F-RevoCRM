# TS-11 マイグレーション

対象: `setup/migration/scripts/` に追加された5本

| # | ファイル | 内容 |
|---|---|---|
| M-01 | `20260616034423_add_documents_to_menu` | Documents をアプリメニューに登録（ツールのみ表示ON） |
| M-02 | `20260616125641_setup_documents_compliance_schema` | 監査ログ／ファイルバージョンのテーブル作成、ファイルサイズの `BIGINT` 化、`indexed_content` 追加、電帳法の項目定義（ブロック・ピックリスト含む） |
| M-03 | `20260617153210_setup_documents_folders` | フォルダの階層化（`parent_folderid`）、`vtiger_folder_permissions` 作成＋既存フォルダに既定権限 |
| M-04 | `20260806084006_setup_holidays_master` | `vtiger_holidays` / `vtiger_holiday_settings` 作成、設定メニュー登録（別ブロックにあれば移動）、祝日の初期投入、週休設定の移行 |
| M-05 | `20260806102225_setup_documents_settings` | `vtiger_documents_settings` 作成＋既定値（入力期限・書類区分ごとの取引モジュール）、cron 登録、設定メニュー追加、既存ドキュメントの期限計算と適合判定 |

> 当初は機能ごとに13本に分かれていたが、1機能あたり複数本を順に流す構成は
> 適用順序の前提が読み取りにくく、部分適用時の状態も追いにくいため、
> 機能単位の5本へ統合した。統合前後で適用結果（スキーマ・項目・設定・メニュー・cron）は同一。

## 1. 期待される振る舞い（仕様）

| # | 仕様 |
|---|---|
| S-01 | 既にカラム／テーブル／項目／メニューが存在する場合はスキップし、二重適用しない |
| S-02 | 前提となる対象（テーブル・ブロック・モジュール）が無い場合は、落ちずにスキップかエラーを明示する |
| S-03 | 適用順序（タイムスタンプ順）どおりに実行して整合が取れる |
| S-04 | 適用後、アプリケーションが参照するテーブル・カラム・設定・メニュー・cron が揃っている |
| S-05 | 適用ログに、実行した内容とスキップ理由が出る |
| S-06 | 既存データを破壊しない（型変更で値が欠落しない、既存の設定を上書きしない） |

## 2. デシジョン表

### DT-1: `M-02`（`BIGINT` 拡張）の1カラムあたりの処理

| 条件 | R1 | R2 | R3 | R4 |
|---|---|---|---|---|
| 対象テーブルが存在 | − | ✓ | ✓ | ✓ |
| 対象カラムが存在 | * | − | ✓ | ✓ |
| 既に `BIGINT` | * | * | ✓ | − |
| **結果** | スキップ（ログ） | スキップ（ログ） | スキップ（ログ） | `ALTER TABLE ... MODIFY` |
| 対応ケース | TC-MG-051 | TC-MG-052 | TC-MG-053 | TC-MG-050 |

> `vtiger_notes_file_versions` は同じマイグレーションが作成するため、
> 新規適用では最初から `BIGINT`（R3 相当）になる。R4 は `INT` で作成済みの既存環境向け。

### DT-2: `M-04`（休祝日マスタ）の各処理

| 条件 | R1 | R2 | R3 |
|---|---|---|---|
| `vtiger_holidays` が存在 | ✓ | − | − |
| 設定メニュー `LBL_HOLIDAYS` が存在 | * | ✓ | − |
| `LBL_CONFIGURATION` ブロックが存在 | * | * | ✓ |
| **テーブル作成** | スキップ | 作成 | 作成 |
| **メニュー登録** | 判定は独立 | 同じブロックならスキップ／別ブロックなら移動 | 登録 |
| **祝日の初期投入** | `INSERT IGNORE` で重複しない | 同左 | 同左 |
| 対応ケース | TC-MG-081 | TC-MG-082 / TC-MG-100 | TC-MG-080 |

> `LBL_CONFIGURATION` ブロックが無い環境では、メニュー登録のみスキップしテーブル作成と初期投入は続行する（TC-MG-083）。

### DT-3: `M-04`（週休設定）の初期値

| 条件 | R1 | R2 |
|---|---|---|
| `config.customize.php` に `$business_week_holidays` がある | ✓ | − |
| **`weekly_holidays` の初期値** | config の値を移行 | 既定（`0,6`） |
| 対応ケース | TC-MG-091 | TC-MG-090 |

## 3. 境界値・同値分割

| 対象 | 境界 | 期待 |
|---|---|---|
| M-02 | `filesize` に 2,147,483,647 が入っている | 変換後も値が保持される |
| M-02 | `filesize` が `NULL` | `NULL` のまま |
| M-02 | 既に `BIGINT` | 何もしない |
| M-04 | 実行年が 2020 未満（システム日付を過去に設定） | `SUPPORTED_FROM_YEAR` にクランプされる |
| M-04 | 初期投入年数 | 前年から4年分 |
| M-04 | 同じ日付が既に登録済み | `INSERT IGNORE` でスキップ |
| M-05 | 既存ドキュメント0件 | 初期化処理が0件で正常終了 |
| M-05 | cron タスクが既に登録済み | 重複登録しない |
| M-03 | フォルダ0件 | 既定権限の付与が0件で正常終了 |

## 4. テストケース

自動化されたケースは `test/unit/migration/test_documents_migrations.php` で検証する。
ブランチ適用前のダンプ（`e2e/fixtures/e2e_dump.sql`）から使い捨ての DB を作って実際に適用するため、
開発 DB には影響しない。

```bash
php test/unit/migration/test_documents_migrations.php          # 実行後に使い捨て DB を削除する
php test/unit/migration/test_documents_migrations.php --keep   # 調査用に DB を残す
```

### 4.1 共通

| ID | 観点 | 前提 | 手順 | 期待結果 | 自動化 |
|---|---|---|---|---|---|
| TC-MG-001 | 正常系（S-03/S-04） | 本ブランチ適用前の DB | 全マイグレーションを順に適用 | すべて成功。エラーなし | 自動（テスト1） |
| TC-MG-002 | 冪等性（S-01） | TC-MG-001 の直後 | 適用済み管理をクリアして再実行 | 二重適用されない。**すべてのマイグレーションが内部でも存在チェックを持つ**（適用済み管理に依存しない） | 自動（テスト2） |
| TC-MG-003 | 正常系（S-05） | − | 適用ログを確認 | 実行内容とスキップ理由が読み取れる | 自動（テスト2） |
| TC-MG-004 | 結合（S-04） | 適用後 | 一覧・詳細・設定画面・cron を実行 | いずれもエラーにならない | 手動 |
| TC-MG-005 | 部分適用 | 一部だけ適用済みの DB | 残りを適用 | 既存分をスキップし、不足分だけ適用される | 自動（テスト3・4） |
| TC-MG-006 | 新規インストール | 空の DB にインストール後 | 全適用 | 成功する（既存環境と同じスキーマになる） | 手動 |
| TC-MG-007 | 統合前との等価性 | 本ブランチ適用前の DB | 統合前の13本と統合後の5本をそれぞれ別 DB に適用して比較 | スキーマ・項目定義・ピックリスト・設定・メニュー・cron が一致する | 手動（統合時に実施済み） |

### 4.2 個別

| ID | 対象 | 観点 | 手順 | 期待結果 | 自動化 |
|---|---|---|---|---|---|
| TC-MG-010 | M-01 | 正常系 | 適用 | `vtiger_app2tab` に Documents が登録され、ツールのみ表示ON | 自動（テスト1） |
| TC-MG-011 | M-01 | 異常系（S-02） | `vtiger_tab` に Documents が無い状態で適用 | 明示的な例外メッセージ（原因が分かる）。ロールバックされ中途半端に登録されない | 自動（テスト8） |
| TC-MG-012 | M-01 | 冪等性 | 再適用 | 「登録済みのためスキップ」 | 自動（テスト2） |
| TC-MG-020 | M-02 | 正常系 | 適用 | `vtiger_notes` に電帳法カラムが追加され、項目定義（`vtiger_field`）とブロック・ピックリストも登録される | 自動（テスト1） |
| TC-MG-021 | M-02 | 正常系 | 適用後に編集画面 | 追加項目がレイアウトに現れ、保存できる | 手動 |
| TC-MG-022 | M-02 | 冪等性 | 再適用 | 既存カラム・項目・ブロック・ピックリストをスキップし、重複しない | 自動（テスト2） |
| TC-MG-023 | M-02 | 既存データ（S-06） | 既存ドキュメントあり | 既存レコードの値が壊れない（追加カラムは NULL） | 手動 |
| TC-MG-030 | M-02 | 正常系 | 適用 | `vtiger_notes_audit_log` / `vtiger_notes_file_versions` が作られ、インデックスがある | 自動（テスト1） |
| TC-MG-030b | M-02 | 冪等性（S-01） | 適用済み管理をクリアして再実行 | 両テーブルとも「既に存在するためスキップ」。エラーにならない | 自動（テスト2） |
| TC-MG-030c | M-02 | 部分適用（S-01） | 片方のテーブルだけ存在する状態で実行 | 存在する方をスキップし、無い方だけ作成する | 自動（テスト4） |
| TC-MG-031 | M-02 | 結合 | 適用後にドキュメント保存 | 監査ログとバージョンが記録される | 手動 |
| TC-MG-050 | M-02 | 正常系（DT-1 R4） | `INT` の状態で適用 | `vtiger_notes.filesize` と `vtiger_notes_file_versions.file_size` が `BIGINT` になる | 自動（テスト3） |
| TC-MG-051 | M-02 | 境界値（DT-1 R1） | `vtiger_notes_file_versions` が無い環境 | テーブルを作り直し、`file_size` は最初から `BIGINT` になる | 自動（テスト3） |
| TC-MG-052 | M-02 | 境界値（DT-1 R2） | カラムが無い | スキップ | 自動（テスト3） |
| TC-MG-053 | M-02 | 冪等性（DT-1 R3） | 再適用 | 「既に BIGINT のためスキップ」 | 自動（テスト3） |
| TC-MG-054 | M-02 | 既存データ（S-06） | `filesize` に 2,147,483,647 / NULL / 0 | 値が保持される | 自動（テスト3） |
| TC-MG-055 | M-02 | 結合 | 適用後に 2GB のファイルを登録 | `filesize` が正しく保存・表示される | 手動 |
| TC-MG-070 | M-02 | 正常系 | 適用 | `indexed_content` が追加される | 自動（テスト1） |
| TC-MG-071 | M-02 | 結合 | 適用後に新規登録 | 抽出テキストが保存され、検索できる | 手動 |
| TC-MG-072 | M-02 | 既存データ | 既存ドキュメント | `indexed_content` は空。`reindex_documents.php --execute` で作成できる | 手動 |
| TC-MG-040 | M-03 | 正常系 | 適用 | `vtiger_folder_permissions` が作られる | 自動（テスト1） |
| TC-MG-040b | M-03 | 冪等性（S-01） | 適用済み管理をクリアして再実行 | 「既に存在するためスキップ」。既定権限の再投入も行わない | 自動（テスト2） |
| TC-MG-041 | M-03 | 正常系 | 既存フォルダあり | 各フォルダに `everyone/edit` が付与され、一般ユーザーの既存の見え方が変わらない（S-06） | 自動（テスト1）＋手動 |
| TC-MG-042 | M-03 | 境界値 | フォルダ0件 | エラーにならない | 手動 |
| TC-MG-060 | M-03 | 正常系 | 適用 | `parent_folderid` が追加され、既定 0（ルート） | 自動（テスト1） |
| TC-MG-061 | M-03 | 冪等性 | 再適用 | スキップ | 自動（テスト2） |
| TC-MG-062 | M-03 | 既存データ | 既存フォルダ | すべてルート扱いになり、一覧が壊れない | 手動 |
| TC-MG-080 | M-04 | 正常系（DT-2 R3） | 適用 | テーブル作成＋メニュー登録＋前年から4年分の祝日が入る | 自動（テスト1） |
| TC-MG-081 | M-04 | 冪等性（DT-2 R1） | 再適用 | テーブル作成をスキップし、祝日は `INSERT IGNORE` で増えない | 自動（テスト2） |
| TC-MG-082 | M-04 | 冪等性（DT-2 R2） | メニュー登録済み（システム構成） | メニュー登録をスキップし、並び順も変えない | 自動（テスト6） |
| TC-MG-083 | M-04 | 異常系（S-02） | `LBL_CONFIGURATION` ブロックが無い | メニュー登録のみスキップし、他は続行 | 自動（テスト5） |
| TC-MG-084 | M-04 | 既存データ（S-06） | 手動登録した休日と同じ日付 | 上書きされない | 手動 |
| TC-MG-090 | M-04 | 正常系（DT-3 R2） | config に指定なし | `weekly_holidays = '0,6'` | 自動（テスト1） |
| TC-MG-091 | M-04 | 正常系（DT-3 R1） | `$business_week_holidays = [0]` | `weekly_holidays = '0'` に移行される | 自動（テスト7） |
| TC-MG-092 | M-04 | 冪等性 | 再適用 | 既存の設定値を上書きしない | 自動（テスト7） |
| TC-MG-100 | M-04 | 正常系 | 「他の設定」に登録済みの状態で適用 | 「システム構成」に移動する（追加ではない） | 自動（テスト6） |
| TC-MG-101 | M-04 | 境界値 | 既に「システム構成」 | 何もしない | 自動（テスト6） |
| TC-MG-102 | M-04 | 境界値 | メニュー自体が無い | 新規登録される | 自動（テスト5・6） |
| TC-MG-110 | M-05 | 正常系 | 適用 | `vtiger_documents_settings` が作られ、既定値が入る | 自動（テスト1） |
| TC-MG-111 | M-05 | 正常系 | 適用 | cron タスク `DocumentsInputDeadlineStatus` が登録される | 自動（テスト1） |
| TC-MG-112 | M-05 | 冪等性 | 再適用 | 設定・cron が重複しない | 自動（テスト2） |
| TC-MG-113 | M-05 | 正常系 | スキャナ保存＋受領日ありの既存ドキュメント | 適用後に入力期限が設定されている | 手動 |
| TC-MG-114 | M-05 | 境界値 | 対象ドキュメント0件 | 正常終了 | 自動（テスト1） |
| TC-MG-115 | M-05 | 結合 | 適用後 | cron が実行でき、期限状態が更新される（TS-03 TC-DL-066） | 手動 |
| TC-MG-120 | M-05 | 正常系 | 適用 | 「システム構成」に電帳法設定のメニューが追加される | 自動（テスト1） |
| TC-MG-121 | M-05 | 冪等性 | 再適用 | スキップ | 自動（テスト2） |
| TC-MG-122 | M-05 | 結合 | メニューから遷移 | 設定画面が開く（TS-04） | 手動 |
| TC-MG-130a | M-05 | 正常系 | 適用 | `vtiger_documents_settings` に `compliance_transaction_modules` が登録され、値が既定の JSON | 自動（テスト1） |
| TC-MG-130c | M-05 | 冪等性（S-01） | 再適用 | 「既に登録済みのためスキップ」。**再判定も走らない** | 自動（テスト2・9） |
| TC-MG-130d | M-05 | 既存データ（S-06） | 手動で設定を変更済み | 上書きされない | 自動（テスト9） |
| TC-MG-130e | M-05 | 回帰 | 書類区分 `contract` を `ServiceContracts` に紐づけた既存ドキュメント | 適用後に `compliant` へ変わる（従来の不具合が解消される） | 手動 |
| TC-MG-130f | M-05 | ログ | 適用 | 区分ごとの既定モジュールと再判定件数（対象/適合/不適合）がログに出る | 手動 |
| TC-MG-130g | M-05 | 性能 | 電帳法対象1万件 | `batchCheck()` の所要時間を計測する ⚠️ 要確認（マイグレーション内で全件再判定する） | 手動 |

### 4.3 環境差

| ID | 観点 | 前提 | 期待結果 | 自動化 |
|---|---|---|---|---|
| TC-MG-130 | DB バージョン | MySQL 8.0 | すべて成功 | 手動 |
| TC-MG-131 | DB バージョン | MySQL 8.4 | すべて成功（外部キー・DEFAULT の構文差で失敗しない） | 自動（テスト環境が 8.4） |
| TC-MG-132 | SQL モード | `STRICT_TRANS_TABLES` 有効 | `DATETIME DEFAULT CURRENT_TIMESTAMP` 等でエラーにならない | 手動 |
| TC-MG-133 | 照合順序 | 既存テーブルが `utf8` の環境 | 新規テーブルの `utf8mb4` と JOIN してもエラーにならない ⚠️ 要確認 | 手動 |
| TC-MG-134 | 大量データ | `vtiger_notes` 100万件 | M-02 の `ALTER TABLE` の所要時間・ロック時間を計測する ⚠️ 要確認（無停止で流せるか） | 手動 |

## 5. 検討したが対象外とした観点（N/A）

| 観点 | 判断 |
|---|---|
| ロールバック（down マイグレーション） | N/A: `FRMigrationClass` は前進のみ。切り戻しは DB バックアップからの復元を前提とする |
| 並行実行（同時に2プロセスが適用） | N/A: 運用上、適用は1回・単一プロセスで行う |
| SQL インジェクション | N/A: すべてプレースホルダまたは定数由来のテーブル名 |
| 旧 TC-MG-130b（設定テーブルが無い状態で取引モジュールを登録） | N/A: 統合により同一マイグレーション内でテーブルを作るため、この状態は発生しない |
